import Region

from random import randint

class ConvexPolygon(Region):
    def __init__(self, image, map, sides, bound=None)
        self.image = image
        self.bounding_region = bound
        self.map = map
        self.sides = sides

        self.interior = set([])
        self.open_exterior = set([])
        self.closed_exterior = set([])

    def draw_polygon():
        count = 0
        while count < self.sides:
            count += 1
        return

    def get_next_line():
        return
